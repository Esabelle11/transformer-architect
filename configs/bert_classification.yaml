
project_name: "BERT"

tokenizer_model_name: "bert-base-uncased"

#dataset size
train_size: 1000
test_size: 250
max_length: 512


#training config
batch_size: 16
lr: 0.0001
epochs: 3

# model
d_model: 256
n_heads: 4
n_layers: 4
n_classes: 2
dropout: 0.1